# .github/workflows/secure-secret-demo.yml
name: "Secure Secrets Demo"

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  safe_job:
    runs-on: ubuntu-latest
    steps:
      - name: Do not expose secrets
        run: echo "No secrets exposed"
